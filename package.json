{
  "scripts": {
    "postinstall": "npx playwright install --with-deps",
    "lint:fix": "npx prettier --write .",
    "test:localhost": "ENVIRONMENT=localhost NODE_TLS_REJECT_UNAUTHORIZED=0 npx playwright test --ui --grep-invert \"@service:wordpress @requires:aws\"",
    "test:develop": "ENVIRONMENT=develop npx playwright test --ui --grep-invert \"@wip\"",
    "test:staging": "ENVIRONMENT=staging npx playwright test --ui --grep-invert \"@wip\"",
    "test:production": "ENVIRONMENT=production npx playwright test --ui --grep-invert \"@wip\""
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@estruyf/github-actions-reporter": "^1.9.2",
    "@fulldecent/nice-checkers-plugin": "^1.3.8",
    "@playwright/test": "^1.60.0",
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "dotenv": "^17.2.3",
    "html-validate": "^11.4.0"
  }
}
